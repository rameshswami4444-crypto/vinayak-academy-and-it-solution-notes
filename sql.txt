-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.students (
  id text NOT NULL,
  password text NOT NULL,
  course text,
  session_id text,
  fees_status text,
  due_date date,
  payment_note text,
  name text,
  father_name text,
  mobile numeric,
  email text,
  address text,
  course_id uuid DEFAULT gen_random_uuid(),
  batch_id uuid DEFAULT gen_random_uuid(),
  admission_date date,
  account_status text,
  created_at timestamp without time zone,
  alternate_mobile numeric,
  batch text,
  course_duration text,
  failed_attempts integer DEFAULT 0,
  locked_until timestamp with time zone,
  last_failed_login timestamp with time zone,
  CONSTRAINT students_pkey PRIMARY KEY (id)
);
CREATE TABLE public.courses (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  course_name text NOT NULL,
  duration text,
  total_fee numeric,
  description text,
  created_at timestamp with time zone,
  institute_id uuid,
  CONSTRAINT courses_pkey PRIMARY KEY (id),
  CONSTRAINT courses_institute_id_fkey FOREIGN KEY (institute_id) REFERENCES public.institutes(id)
);
CREATE TABLE public.batches (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  batch_name text NOT NULL,
  course_id uuid DEFAULT gen_random_uuid(),
  start_date date,
  end_date date,
  timing text,
  institute_id uuid,
  CONSTRAINT batches_pkey PRIMARY KEY (id),
  CONSTRAINT batches_institute_id_fkey FOREIGN KEY (institute_id) REFERENCES public.institutes(id)
);
CREATE TABLE public.student_fees (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  student_id text NOT NULL,
  total_fee numeric,
  admission_fee numeric,
  remaining_fee numeric,
  total_emis bigint,
  status text,
  paid_amount numeric,
  institute_id uuid,
  CONSTRAINT student_fees_pkey PRIMARY KEY (id),
  CONSTRAINT student_fees_institute_id_fkey FOREIGN KEY (institute_id) REFERENCES public.institutes(id)
);
CREATE TABLE public.emis (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  student_id text NOT NULL,
  emi_number smallint,
  amount numeric,
  due_date date,
  paid_date date,
  status text,
  payment_id uuid DEFAULT gen_random_uuid(),
  institute_id uuid,
  CONSTRAINT emis_pkey PRIMARY KEY (id),
  CONSTRAINT emis_institute_id_fkey FOREIGN KEY (institute_id) REFERENCES public.institutes(id)
);
CREATE TABLE public.payments (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  student_id text NOT NULL,
  emi_id uuid DEFAULT gen_random_uuid(),
  amount numeric,
  payment_mode text,
  transaction_id text,
  payment_date timestamp without time zone,
  remark text,
  institute_id uuid,
  CONSTRAINT payments_pkey PRIMARY KEY (id),
  CONSTRAINT payments_institute_id_fkey FOREIGN KEY (institute_id) REFERENCES public.institutes(id)
);
CREATE TABLE public.notes (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  course_id uuid NOT NULL DEFAULT gen_random_uuid(),
  subject text,
  title text,
  file_path text,
  created_at timestamp without time zone,
  institute_id uuid,
  CONSTRAINT notes_pkey PRIMARY KEY (id),
  CONSTRAINT notes_institute_id_fkey FOREIGN KEY (institute_id) REFERENCES public.institutes(id)
);
CREATE TABLE public.assignments (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  course_id uuid NOT NULL DEFAULT gen_random_uuid(),
  title text,
  description text,
  due_date date,
  file_url text,
  institute_id uuid,
  CONSTRAINT assignments_pkey PRIMARY KEY (id),
  CONSTRAINT assignments_institute_id_fkey FOREIGN KEY (institute_id) REFERENCES public.institutes(id)
);
CREATE TABLE public.announcements (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  title text NOT NULL,
  message text,
  target_course uuid DEFAULT gen_random_uuid(),
  created_at timestamp without time zone,
  institute_id uuid,
  CONSTRAINT announcements_pkey PRIMARY KEY (id),
  CONSTRAINT announcements_institute_id_fkey FOREIGN KEY (institute_id) REFERENCES public.institutes(id)
);
CREATE TABLE public.attendance (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  student_id text NOT NULL,
  date date,
  status text,
  institute_id uuid,
  CONSTRAINT attendance_pkey PRIMARY KEY (id),
  CONSTRAINT attendance_institute_id_fkey FOREIGN KEY (institute_id) REFERENCES public.institutes(id)
);
CREATE TABLE public.admins (
  username text NOT NULL,
  password text NOT NULL,
  role text,
  institute_id uuid,
  full_name text,
  created_at timestamp without time zone DEFAULT now(),
  subscription_plan text DEFAULT 'starter'::text,
  subscription_start date,
  subscription_end date,
  account_status text DEFAULT 'active'::text,
  status text,
  failed_attempts integer DEFAULT 0,
  locked_until timestamp with time zone,
  last_failed_login timestamp with time zone,
  CONSTRAINT admins_pkey PRIMARY KEY (username)
);
CREATE TABLE public.settings (
  institute_name text NOT NULL,
  logo_url text NOT NULL,
  payment_qr text,
  contact_number text,
  whatsapp text,
  address text,
  institute_id uuid,
  CONSTRAINT settings_pkey PRIMARY KEY (institute_name),
  CONSTRAINT settings_institute_id_fkey FOREIGN KEY (institute_id) REFERENCES public.institutes(id)
);
CREATE TABLE public.material_courses (
  id bigint NOT NULL DEFAULT nextval('material_courses_id_seq'::regclass),
  note_id bigint NOT NULL,
  course_id uuid NOT NULL,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT material_courses_pkey PRIMARY KEY (id),
  CONSTRAINT material_courses_note_id_fkey FOREIGN KEY (note_id) REFERENCES public.notes(id),
  CONSTRAINT material_courses_course_id_fkey FOREIGN KEY (course_id) REFERENCES public.courses(id)
);
CREATE TABLE public.institutes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  institute_name text NOT NULL,
  institute_code text UNIQUE,
  owner text,
  email text,
  contact text,
  logo_url text,
  primary_color text DEFAULT '#4F46E5'::text,
  status text DEFAULT 'active'::text,
  domain text,
  subdomain text,
  created_at timestamp without time zone DEFAULT now(),
  subscription_plan text DEFAULT 'starter'::text,
  subscription_start date,
  subscription_end date,
  account_status text DEFAULT 'active'::text,
  outstanding_amount numeric,
  payment_status text,
  renewal_date date,
  payment_notes text,
  subscription_amount numeric,
  CONSTRAINT institutes_pkey PRIMARY KEY (id)
);
CREATE TABLE public.attendance_sessions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  course_id uuid,
  subject text NOT NULL,
  lecture_title text NOT NULL,
  duration_minutes integer DEFAULT 5,
  start_time timestamp with time zone NOT NULL,
  end_time timestamp with time zone NOT NULL,
  status text DEFAULT 'OPEN'::text,
  created_by text,
  created_at timestamp with time zone DEFAULT now(),
  session_id text,
  CONSTRAINT attendance_sessions_pkey PRIMARY KEY (id),
  CONSTRAINT attendance_sessions_course_id_fkey FOREIGN KEY (course_id) REFERENCES public.courses(id)
);
CREATE TABLE public.attendance_responses (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  session_id uuid,
  student_id text,
  response text,
  response_time timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT attendance_responses_pkey PRIMARY KEY (id),
  CONSTRAINT attendance_responses_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.attendance_sessions(id),
  CONSTRAINT attendance_responses_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.students(id)
);
